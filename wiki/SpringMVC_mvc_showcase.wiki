#summary sample

 * [http://home:8080/mvc-showcase mvc-showcase]
 * Uses jquery to send an Ajax request to application to exercise a mapping rule ({{{ @RequestMapping(...) }}}) to show that it works.
 * An acceptance test suite for Spring MVC.